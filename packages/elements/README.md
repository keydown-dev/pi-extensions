# @keydown-dev/elements

Placeholder for shared TUI elements used by Keydown Pi extensions.

This package should stay Pi-extension-free: no `pi.extensions` manifest, no tool registration, and no session state. It should expose reusable panel, progress-list, footer-hint, wrapping, and color helpers.

See `../../plans/monorepo-extensions.md`.
