import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function todoListExtension(_pi: ExtensionAPI): void {
  // Placeholder: register the todo tool and compact/expanded widget here.
}
