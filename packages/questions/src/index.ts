import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function askUserQuestionExtension(_pi: ExtensionAPI): void {
  // Placeholder: register the structured Q&A tool and widget here.
}
