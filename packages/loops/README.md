# @keydown-dev/pi-loops

Future workspace home for the existing loop extension, renamed as `pi-loops`.

For now, the working package still lives at the repository root. The planned move and self-run/subagent execution modes are documented in `../../plans/monorepo-extensions.md`.
