{
  "name": "@keydown-dev/pi-subagent-loop",
  "version": "0.0.1",
  "description": "Subagent Loop fresh-context worker orchestration for Pi with git checkpoints and handoff artifacts.",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi",
    "subagent",
    "ralph",
    "wiggum",
    "orchestrator",
    "subagents",
    "fresh-context",
    "loop"
  ],
  "license": "MIT",
  "pi": {
    "extensions": [
      "./extensions"
    ],
    "skills": [
      "./skills"
    ]
  },
  "scripts": {
    "test": "node --import tsx --test \"tests/*.test.ts\" \".tmp/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typebox": "^1.0.55",
    "typescript": "^5.8.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
